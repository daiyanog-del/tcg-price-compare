# TASKS — CardPriceChecker (TCGYM)

> MAMB運用のタスクボード。セッション開始時にまずこれを読み、終了時に更新する。
> 現在地の詳細は [docs/activeContext.md](docs/activeContext.md)、決定の経緯は [docs/decisions.md](docs/decisions.md)。

## 進行中

- [ ] **★一人回しスマホ最適化 フェーズ1〜3（2026-09-03 実装完了・ユーザー実機検収待ち）** — 設計書 docs/design-solitaire-mobile-2026-09-02.md §8/§9。フェーズ1（1カラム・6列52px・縦タブ・タップ選択・アクションシート）→実機検収3点（多重読込／デッキボタンが1ドロー／共有方法不明）→フェーズ2（デッキ一覧シート＋検索＋複数選択→手札へ）・フェーズ3（共有シート＝リンク作成→OS共有/コピー/X投稿、初見CTA）＋多重読込防止（PC共通）を同日に実装。reviewer 計4ラウンド。**残＝実機検収（iOS Safari / Android Chrome）: ①想定外のカードが動かない ②墓地の横スクロールとカードの引き出しが両立 ③回転で崩れない ④共有シートの「コピー」「Xに投稿」「共有…」が実機で動く ⑤一覧シートで検索中にキーボードが「手札へ」を隠さない ⑥⋯メニュー「すべて消去」で初期状態に戻り、開き直しても復元されない**。第2次検収（同日夜）: ①③④⑥は OK、②共有は実機で動作（X投稿は未テスト）、横向きは非推奨で合意。追加5点（墓石アイコン非表示／手札・墓地・除外・EX を各1行全幅／一覧の行き先を墓地・除外・EX・盤面へ拡張／移動の演出／共有範囲の指定／端末保存＋保存済み一覧）を実装（設計書 §10）。第3次検収（09-04）: 4行全幅・行き先は OK。範囲指定/保存先が不明・再生でデッキとの出入りが見えない→共有シートを①〜④の番号付き構成に再編＋再生の出入り演出（設計書 §11）。第4次検収（09-04）: 再生ウィンドウが見づらく EX 行に被る／「何手目から」は直感的でない→**録画モデル（● 録画→■ 停止で共有）と再生モード（下部バー切替＋スライダー帯・盤面を再フィット）** を実装（設計書 §12）。**第5次検収待ち**: 録画→停止→共有→受け手で再生モードが開くか／再生モードの見やすさ／録画ボタンの分かりやすさ。**既知（据え置き）**: 「EXへ」やドラッグでメインカードを EX プールへ移すと共有/保存リプレイでは最初から EX に置かれる（既存挙動・設計書 §10）見送り（司令塔判断）: §3.3「個別長押しで盤面へ」／リセット&5ドローの confirm／極小画面の touch-action
- [x] **端末間同期 P1〜P3 完了（2026-08-19 本番E2E確認済み）** — 旧: ★P1の本番E2E確認（2026-08-11デプロイ） — デプロイ後、PCとスマホの両方で購入候補を追加し、`sync_accounts` に行ができる／`wishlist_rev` が増える／もう片方の端末に反映されるかを確認する。設計は docs/design-sync-2026-08-09.md（第2版）、経緯は decisions.md 2026-08-11。**P1時点では画面に見える変化は無い**（同期は裏で動くだけ・QRの導線はP3）。レート制限の閾値（5秒）は `TODO: calibrate from data` 付きの仮置き
- [x] **端末間同期 P2（保存デッキ）＋ init の穴ふさぎ（2026-08-13 完了）** — デッキ同期・一人回しページの合流（localStorage の単一窓口化）・`/api/sync/init` の空/不正本文拒否。pytest 606件全緑。**ニューロン連携のChrome拡張が localStorage のデッキを直接書いているかは未確認**（書いていれば同じ対応がもう1経路必要・P3以降で確認）
- [x] **端末間同期 P3（QR＋ワンタイムリンク）完了（2026-08-19）** — 端末を紐づける導線。`sync.html`（**GAタグを載せない専用テンプレート**＝トークンがクエリ文字列でGoogleに送信されるのを防ぐ）・robots.txt に `/sync` を追加・引き換え前の確認画面（逆方向フィッシング対策）・QR生成ライブラリを static に同梱（外部CDN不可）。テーブル `sync_link_tokens` は作成済み

- [ ] **★新弾マトリクス初回運用の確認（2026-08-09朝〜）** — tcg-collect-featured（JST10:15）の初回実行ログで①403で失敗した店舗の有無（**10:15時点のブロック解除は未実測**。8/8のJST11:15実測では両店200 OK）②ブロック様判定・再試行の発動状況③所要時間、を確認。/featured のマトリクス表示（ヒートマップ・型番・「+ 候補」→購入リスト連携）もブラウザで目視確認。BLOCKLIKE_FAIL_RATIO=30% と _CHUNK_BYTE_BUDGET=6000 は `TODO: calibrate from data` 付きの暫定値（実績が溜まったら校正）。経緯は decisions.md 2026-08-08
- [ ] **★X自動投稿の再開（2026-08-09目安・境界修正のデプロイで後ろにずらした）** — 他TCG混入の根治（2026-08-03デプロイ）で**遊戯王の取りこぼしが解消して新規取得が増える**（青眼の白龍 販売60→90件・買取28→57件）ため、第2弾・第4弾と同じ「見かけの急落」が出うる。デプロイは当日の日次収集（JST5:00）より後なので**新実装での初回収集は8/4**、4回走り終えるのが8/7、比較窓(3日)から変更前の価格が抜けるのが8/8。**さらに 2026-08-04 JST21:22 に境界チェック修正をデプロイした**（別カード・グッズが抜けるので min_price は上がる方向＝見かけの急騰が出うる）。新実装での初回収集は8/5、4回走り終えるのが8/8、比較窓が抜けるのは**8/9**。post-x.yml の schedule は停止中。戻す前に /api/movers の上位に見かけの急落が残っていないか確認する。**買取側は増加90件でガード②が無いため、Web表示の買取値上がりランキングも見ること**（経緯は docs/decisions.md 2026-08-03）。**追記(8/7): カードラッシュが8/5から403遮断で取得半減中**（偽変動はガード①=共通店舗比較が防ぐが、カバレッジ低下で候補が減る方向。再開判断時に当日の取得状況も見ること）
- [ ] **切替日の目視監視（2026-08-04〜05）** — 修正後の初回収集でトレコロ／カーナベルの新規取得分が price_history に入る。/api/movers 上位に偽の急落が混ざっていないか、collection_runs に想定どおり記録されているかを確認。**境界修正ぶんは偽の急騰（min_price 上昇）にも注意し、「融合」は名指しで確認する**（カードラッシュ販売が506件→0件になり価格系列が総入れ替えになるため）

## バックログ（優先度順）

- [ ] **画像プロキシのContent-Type透過（公開前修正 第2バッチ 2026-08-20 reviewer指摘・実効リスク低）** — `app.py:3516`付近の `/api/card-image-proxy`（カーナベル画像プロキシ）は、上流（カーナベル）レスポンスの `Content-Type` ヘッダをそのまま `Response(img_resp.content, content_type=ct)` に透過している。画像URL自体は `kanabell_card_image_url(name)` でサーバー側生成のため直接の攻撃経路は無いが、上流が万一画像以外のcontent-typeを返した場合にそのまま配信してしまう。`image/*` のホワイトリスト検証（不一致なら `image/jpeg` にフォールバック等）を入れるのが望ましい。第2バッチで追加した `X-Content-Type-Options: nosniff` により実効リスクはさらに下がっている

- [ ] **CSP（Content-Security-Policy）の段階導入（公開前修正 第2バッチ 2026-08-20起票）** — セキュリティヘッダ（X-Content-Type-Options / Referrer-Policy / X-Frame-Options）は本バッチで追加済みだが、CSPはインラインscript/onclickを多用しているため未導入。`unsafe-inline` を許容した緩いCSPから始め、nonce方式やイベントハンドラの外出しを段階的に進める設計が必要
- [ ] **href/imgのスキーム検証(safeUrl)が未適用の残存箇所（公開前修正 第2バッチ 2026-08-20 発見・一部修正済み）** — 本バッチで `wish-shop-detail`/featured-matrix/meta recipeリンク等の主要箇所は `safeUrl()` を適用したが、`affLink()`/`safeUrl()` を経由しない生の `href="${esc(url)}"` が他にも残っている可能性がある（今回は明示的に指摘された既知L-1＋発見時に同一パターンで修正したもののみ対応）。全htmlのhref/src属性を棚卸しし、外部URLを描画する箇所は例外なく `safeUrl()` を通す方針に統一するのが望ましい

- [x] **【完了 2026-08-19】`calcWishEstimate()` の照合キー不一致を修正** — サーバ正本(`entries`)へ統一。本番実測: 入力`{ブラックマジシャン, ウルトラレア}`→サーバ`{ブラック・マジシャン, ウルトラ}`で従来は突合失敗し総額0円だったものが、修正後は1/1枚取得・¥40（トレコロCB ¥20×2）と正しく計上されることを確認。突合ロジックは `static/wish-estimate.js` に切り出し、`tests/js/wish_estimate_check.js` で実物を検証。旧記録: 【重要・同根未修正】`calcWishEstimate()`（templates/index.html:4090-4096, 4165, 4186）に、wishCheapestStores()の照合キー不一致バグと同一の根本原因が残存（2026-08-19レビュー記録）** — `const e=list[i]`（localStorage の生データ）を、サーバ返却の補正済み `it.name` ／ 正規化済み `it.rarity_pref` と突合しているため、`rarity:"ウルトラレア"` のような保存値があると Phase1 で突合失敗 → realtime の rarity フィルタ（:4165）でも `info.rarity!==entry.rarity` が必ず不一致 → 画面に「ウルトラレアデータなし」と表示され、DBに価格があるのに見積総額から丸ごと落ちる。`_correct_cardname()` による名前補正でも同様に外れる。修正方針は今回と同じく `resp.entries`（サーバ正本）を起点に組み直す
- [ ] **購入候補の照合キーが区切り文字なしの連結（低・2026-08-19記録）** — `_wishEntryKey`（index.html）と `wish-estimate.js` の `entryKey` は `name+rarity` を区切り無しで連結しているため、理論上 `name="A",rarity="BC"` と `name="AB",rarity="C"` が衝突しうる。実データで起きる可能性は低いが、同期側の `merge_wishlist` は既にタプルキーにしてあるので、こちらも区切り文字かタプル相当に揃えるのが望ましい
- [x] **（2026-09-02 総点検で解消済みと確認: app.py:1101-1121 に 08-19 のレアリティ別集約あり）【中】`_aggregate_per_shop()`（app.py:980-993）がレアリティを問わず店内最安1件しか返さない（2026-08-19レビュー記録）** — リアルタイム経路の rarity フィルタ（index.html:4515）が「その店に無い」と誤判定する。ある店がノーマル¥100とウルトラ¥3,000を在庫していても返るのはノーマルのみ。「取得できなかったカード」件数が実態より膨らむ。修正には `/api/deck` にレアリティ別の per_shop を足す等のサーバ側変更が必要
- [ ] **【軽微】`api_wish_shop_totals()` の50件打ち切り（app.py:1820 `cards_raw[:50]`）が無警告（2026-08-19レビュー記録）** — `wishAdd()` に上限が無いため購入候補は51件以上になりうる。今回フッターの件数表示もサーバ正本基準になったため、打ち切られたことが画面から一切分からなくなった。サーバが `truncated` フラグを返し、フロントで「上位50種類のみ集計しています」と明示するのが望ましい

- [x] **購入候補リスト: 641〜767px幅のカード名切れ＋タップ対象44px化（2026-08-19 完了）** — 旧記録: — スマホ幅（≦640px）はグリッド化で解決したが、その少し上の中間幅では従来の横並びのままで `.wish-card-name` が潰れる（既存不具合・今回の変更が原因ではないことは前後比較で確認済み）。モバイル用グリッドの適用範囲を広げるか、中間幅用の指定を足すかは要判断。あわせてタップ対象の大きさ（削除×36px・枚数±32px）が既存慣習の44pxに揃っていない点も判断待ち
- [ ] **sync_link_tokens に sync_id の索引が無い（性能・2026-08-18記録）** — 「他の端末と同期中」表示の判定（`sync.is_linked`）が `sync_link_tokens` を `sync_id` で全走査している。主キーは `token`、索引は `expires_at` のみのため、行数が増えると判定クエリが遅くなる。当面は行数が少なく実害なしと想定。索引追加はDDLになるため、`/api/sync/pull` のレスポンスタイムが体感で悪化したら着手する（設計文書 docs/design-sync-2026-08-09.md §7.3 参照）
- [ ] **★★価格ロジック監査（2026-08-06）の発見10件の裁定と修正** — 正解役突合による洗い出し完走。台帳は docs/audit-price-logic-2026-08-06.md。F1=グラフAPI 1000行切り捨て（335枚が6週間欠け・本番実証済み・3例目）/F2=X投稿グラフ同クラス4例目/F3=見積もりRPCは7日底値合計（61%が3日以上前の観測値・定義がリポジトリ外のみ）/F4=新弾X投稿S3はガード済みS1と重なり0（42件中0確認・fallback必ず投稿）/F5=購入候補±%の16%が共通店舗で消える/F6=まとめ買い5.8%に古値（平均乖離26%）/F7=Push通知は完全停止中/F8=range分割2箇所order無し/F9=グラフ平均vs最安min不整合/F10=夜間収集3時間の主因はカードラボerror60%+カードラッシュerror24%。**進捗: F1/F2/F8=修正済み(bcb1a85・本番検証済み=墓穴の指名者3,383行/最終日最新)・不変条件テスト新設済み。F4=修正済み(RPC一本化・無投稿日許容・decisions.md 2026-08-07)→次回post-featured実行ログで「RPC取得行数」行とスキップ/投稿の妥当性を確認すること。F5/F6=修正済み(decisions.md 2026-08-07)。**F3=完了（2026-08-09 本番適用済み。適用前後: 2,715枚不変・合計¥563,042→¥576,988(+2.5%)・サンプル5枚全て最新観測日に更新）**。F9=修正済み(グラフをminへ統一・decisions.md 2026-08-07)。F10=実測完了＋第一弾対策済み(カードラボnum=120化。構造対策はカードラッシュ403タスクと合流し裁定待ち)。残=F7(Push通知の復活or廃止・要ユーザー判断)・中4(wish-pricesのlatest基準がF6と不整合=最新日に観測できた店舗だけのmin。F6と同じ系列最新値方式へ揃える案)**

- [ ] **カタログ巡回: item_count乖離許容幅の校正** — clabo_catalog.py の `_ITEM_COUNT_MIN_GAP=6` / `_ITEM_COUNT_GAP_RATIO=0.005` は仮置き（`TODO: calibrate from data` 付き）。毎晩 collection_runs.shop_stats.catalog.categories に total/seen が残るので、2週間ぶん溜まったら乖離分布から校正する（reviewer低4）
- [ ] **カタログ巡回: 部分成功の採用（中3(a)・裁定待ち）** — 現状は1カテゴリでも失敗すると全カタログ破棄→検索方式2,700リクエストへフォールバック。商品に category を持たせ「成功カテゴリだけ採用・失敗カテゴリの商品を持ちうるカードのみ検索へ」にする案。まず total=None 緩和(中3(b)実装済み)後の実運用で不成立の発生率を見てから判断（reviewer 2巡目・中3）
- [ ] **カタログ巡回: 名寄せ失敗商品サンプルの記録（監視項目③）** — unmatched の大半は非監視カードなので、「監視カードに付くはずなのに付かなかった」を検出する指標（例: 前夜ok→当夜empty に転じたカードのリスト）を shop_stats に足す案。白紙監査の監視項目③の残り
- [ ] **カーナベルの送料無料条件が未確認** — 2026-08-05 のまとめ買い送料対応で、カーナベルだけ**送料無料になる購入金額のしきい値を一次情報で確認できなかった**。検索エンジン上には「4,000円以上で無料」という記述があるが、公式の送料ページ（sendinfo.html）にも販売FAQにも記載が無い。現状は `shipping.py` で「無料条件なし（常に100円）」として扱っている（`free_threshold=None` に `TODO: calibrate from data` 付き）。実際に注文する機会があればカート画面で確認して埋める。送料100円なので順位への影響は小さく優先度は低い
- [ ] **送料テーブルの定期見直し** — `shipping.py` は手動管理（定期スクレイプしない決定）。`SHIPPING_CHECKED_ON = 2026-08-05`。各店が送料改定すると値が古くなるため、半年に一度は出典URLを見て確認日を更新する。とくにカードラボは「ヤマト運輸送料改定に伴う、当店送料変更につきまして」の告知を出しており改定を受けやすい

- [ ] **★★カードラボ＋カードラッシュの403遮断への構造対策（裁定待ち）** — 2026-08-07 実測（decisions.md 同日分・追加調査含む）: 両店とも流量閾値（約1,000〜1,250リクエスト/晩）で遮断され、総カード数2,723を下回るため**リクエスト削減だけでは全量収集に届かない**。カードラッシュは8/5初発（約1,900枚目）→8/6悪化（約1,100枚目）で取得が2,555→1,004枚に半減中。**追加実測で前提を穴埋め済み**: ①ブロックは同日中に解除（JST6:12開始→16:40には両店200 OK・本番Render IPで実測）＝案Bの必要条件OK ②**案Cのブロッカー解消**: 検索ページのmain_categoryセレクトから遊戯王11カテゴリID特定（671-679・1219・739）、**合計20,116品目=172リクエスト/回で全量**（閾値の約1/6）。実装課題=「全商品→カード名」の逆向き名寄せ ③カードラッシュのカタログ経路は**空キーワード検索で存在**（877ページ≒87,700枠・8/8訂正）が、閾値（直近≒1,300）への余裕が1.5倍未満で単独推奨不可。**★白紙監査済み（2026-08-08・docs/audit-403-plan-2026-08-08.md）**: 致命的問題なし・プランP1（カードラボ=案C）は骨格妥当。ただし①カテゴリは**14個**（733トークン・1637限定プロモ・2029その他の漏れをkeyword=遊戯で収穫し直す）②名寄せは事前コンパイル必須（素直な実装は510分CPU・実測）③新旧比較はGAIN方向も④/api/search上書き競合の裁定⑤739/733はparity維持で含める⑥shop_stats再定義、を反映すること。**両店は同一プラットフォーム（ocnk）と判明＝P2（カードラッシュ）は裁定保留し、P1単独投入の翌晩にカードラッシュの遮断位置が後退するか（共有カウンタ仮説の検定）を見てから決める**。見落とし代替案=egress分離・収集順ローテーション。**ocnk/店への直接連絡は保留と裁定（2026-08-09）**: P1で請求量が閾値の1/6になり利益が消失、明示NOをもらうと黙認グレーが黒に確定する非対称リスクが残るため。再検討トリガー=恒久ブロック化 or 事業本格化（文面は decisions.md 2026-08-09 参照）
- [ ] **★403遮断の観測継続（測定2晩分の結果と解釈・2026-08-09記録）** — num=120化後の実測: **8/8朝=カードラボ遮断開始が約2,250枚目へ大幅後退**（ok1908/empty342/error485・初⛔22:32Z=T+151分。訂正済み期待値+30〜80枚を大幅超過）、しかし**8/9朝=約1,042枚目へ逆戻り**（ok893/empty149/error1695）。**夜ごとの分散が大きく、固定リクエスト数閾値では説明できない**（白紙監査H2「機構未識別」を支持。num=120の寄与は分離不能）。**カードラッシュは8/8・8/9とも遮断ゼロ**（ok2567/2577・error0）＝8/5-8/6の遮断は自然解消。P2（カードラッシュ対策）の緊急度は低下・監視継続。次の観測点=P1（カタログ巡回）投入後のカードラボ欠測ゼロ化と、カードラッシュの再発有無
- [x] **【完了 2026-08-20】esc()/escJs() が属性デリミタの `"` をエスケープしない横断課題（reviewer 2026-08-08 低11）** — `escAttr()`（`& < > " '` を実体参照化）を新設し、属性文脈で `esc()`/`escJs()` を使っていた箇所を全数列挙のうえ置換。**`templates/index.html`・`static/featured-matrix.js`・`static/packs.js`・`static/mydeck/deck-edit.js` の4ファイルで完了**（reviewer差し戻しで発見された `packs.js`・`deck-edit.js` の残存分も是正済み）。onclick等でJS文字列をHTML属性に埋める二重文脈は `escAttr(escJs(x))` の重ね掛けに統一。あわせて発見した個別バグ（`escJs()`を`data-card`/`id`等の素の属性値に誤用・`esc()`をonclickのJS文字列文脈に誤用・`safeUrl()`で判定だけして生urlを挿入）も同時に是正。href/imgのスキーム検証未適用箇所（`safeUrl()`未適用）も発見分をあわせて修正、`safeUrl('')`がサイトトップURLを誤って返す穴も塞いだ。`escJs()`に改行(`\n`)/復帰(`\r`)のエスケープも追加（属性値中の生改行でonclick全体がSyntaxErrorになるため）。検証は実際の抽出JSをNode vmで実行し、属性脱出ペイロード（ダブルクォート/シングルクォート双方）で脱出しないこと・4ファイル横断で未ラップの`esc(`/`escJs(`が0件であることを静的走査で確認・正常表示が壊れないことを確認し、`tests/js/xss_escaping_check.js`＋`tests/test_xss_escaping.py`として恒久化（`docs/decisions.md` 参照）
- [ ] **キャッシュのグローバルロック内JSON parse（性能・reviewer低1）** — `_shop_cache_store` は read+parse+dump+write を `_cache_lock`（プロセス全体で1本）内で行う。20店×3,000行の人工データで1store約33ms。gunicorn 1w×8t の全スレッドが共有するため、大きいキャッシュファイルが増えると直列化が効いてくる。カード単位ロック（キーごとの Lock）にすれば解消。本番 .cache の実サイズ分布を見てから判断
- [ ] **ページ送り途中失敗の部分結果を全捨てしている（reviewer低2）** — カードラッシュ/トレコロの5ページ巡回中に1ページでも失敗すると fetch_errors>0 で結果ごと非キャッシュになり、15分間毎回フルスクレイプになる。partial 印で「不完全だが使える」として保存し通常検索では不足扱いにする案がある。実害（flaky店の再スクレイプ頻度）を見てから判断
- [ ] **構造変更由来の「例外なし0件」はキャッシュ層で検出できない（既知の限界）** — fetch_errors はネットワーク層の失敗のみ。セレクタ不一致等での0件は「在庫なし」として15分キャッシュされる。tracker の連続失敗検知と連動した0件TTL短縮が発展案（閾値は実データから導出すること）
- [ ] **カード名DBの半角/全角二重登録の棚卸し（reviewer中-4）** — cardnames_ja.json に「BM－４ボムスパイダー/ＢＭ－４ボムスパイダー」等、同一カードの半角版と全角版が別レコードで二重登録されている（10カード×2表記=20件、fuzzy_key衝突64キーの一部）。fuzzy補正が誤合流回避の安全策で見送るため、この20件は表記ゆれ入力からの補正が効かない（デッキ見積もり・リアルタイム検索とも同挙動）。どちらが正式名称かを公式DBと突合して片方を除去する
- [ ] **price_history に非正規キー（半角ダッシュ・閉じ括弧欠け）が直近7日にも書かれている** — 「大海の伝説-フィッシャーマン」「召喚魔術-「杯」等19種（2026-08-03実測）。全て正規形の行が併存するため読み側の実害はないが、系列分裂のノイズ源。書き込み経路のどこかが名寄せ（P1）を通っていない疑い。発生源を particular に特定して塞ぐ
- [ ] **_load_cardnames に排他ロックが無い（reviewer低-4）** — コールドスタート時に複数スレッドが同時に約1MBのJSON構築を行いうる。_ygores_index_lock（2026-06 OOM対策）と同じ形の未対処。ロックを足すだけ

- [ ] **★遊戯王に存在しないカード名が tracked_cards に登録されている（ペガサス・宿命の決闘）** — 2026-08-03 の他TCG根治で判明。両名とも **cardnames_ja.json（15,522件）に存在せず**、本番 `/api/search` は404を返す。price_history にあった行は全て他TCG由来で、削除後は以後も毎回0件で収集される（夜間に6店×2名の無駄な検索が走る）。`active=false` にするのは容易だが、**tracked_cards への登録経路を直さないと再登録される**（デッキ由来の名前抽出が誤ってこの2つを拾ったと推定・未確認）。発生源の特定とセットで判断すること。他にも同種の誤登録が無いか、tracked_cards × cardnames_ja.json の突合で棚卸しする
- [ ] **カーナベル買取のステンレス14行が残っている** — 他TCG混入の根治で収集側は塞いだが、`buyback_history` の既存14行（ブラック・マジシャン ¥12,000 / 青眼の白龍 ¥6,500）は残す判断をした。買取グラフに金属製記念カードの系列が残る。実害が出たら削除する
- [ ] **駿河屋のページ送り未確認** — 唯一未確認の経路。Bot対策が厳しくSession経由の取得が必要。DEFAULT_SHOPS に含まれないため優先度は低い
- [ ] **買取moversにガード②（同方向2店以上）が無い** — `get_buyback_movers` は意図的にガード②不適用（店舗数が少なく該当ゼロになりやすいため）。買取は `max_price` 集計なので、スクレイパー改善で新規に拾った高値レコードが単店の段差として通ってしまう。買取ランキングは外部発信しない（X投稿は販売のみ）ため緊急度は中だが、Web表示の値上がりランキングにデプロイ直後の偽値上がりが出る可能性がある（2026-08-03 reviewer指摘6）
- [ ] **駿河屋の検索語だけ記号を落とさない（未検証）** — `scrape_surugaya` は `normalize_width` 直呼びで、中黒・ダッシュ・コロンをそのまま投げている。他店で実害が確認された正規化が一切入っていない。DEFAULT_SHOPS外（Bot対策で既定除外）のため優先度は低いが、有効化するなら先に実測が必要（2026-08-03 経路棚卸しで判明）
- [ ] **★カードラボの前置き注記で取りこぼし（既存バグ）** — `_has_name_text_outside_brackets` の除去対象が `〔〕 () [] ☆☆ 「」 『』` だけで `※…※`・`《…》`・`【…】`・`{…}` が入っていない。このためカードラボの「※プレイ用特価品※青眼の白龍」「《未開封》ブラック・マジシャン」が0件扱いになる。**特価品はその店の最安値である可能性が高く、min_price を系統的に押し上げている**。実測（2026-08-04 reviewer 2巡目・実店舗9経路8,311ペア）: 除去対象に `※[^※]*※|《[^》]*》|【[^】]*】|\{[^}]*\}` を足すと **GAIN 15件 / LOST 0件**。当初懸念した「`《…》` が種別欄と衝突する」は否定済み（この関数は prefix にしか当たらず、種別欄は名前の後ろに来るため発火しない）。実装時の注意: `※[^※]*※` は対称ペア前提なので ※ が奇数個の商品名で刈りすぎる可能性がある（2026-08-04 reviewer指摘4）
- [x] **（失効: 2026-08-06 に tests.yml を新設済み。2026-09-02 総点検で確認）pytest を実行するCIが1本も無い** — `.github/workflows/` 全10本を確認したが push/PR トリガのテスト実行が存在しない。このため 2026-08-04 に追加した「マスタの『』基底名衝突」不変条件テストは、毎週月曜の update-cardnames.yml による自動更新では働かず、手元で pytest を回したときだけ検知する。update-cardnames.yml のコミット前に pytest ステップを足すか、push/PR で回すCIを1本作る（2026-08-04 reviewer指摘）
- [ ] **CLOUD_IDの3分割形式に `_kanabell_es_host()` が対応していない（堅牢性）** — `KANABELL_CLOUD_ID.split(":")[1]` を base64 デコードする実装で、`名前:中間セグメント:base64` の3分割形式だとペイロード（最終セグメント）を取り違えて `binascii.Error` になり、例外を握って**検索を丸ごとスキップ**する。2026-08-04のローカル検証で遭遇。**本番は無事**（Renderログで tcg-collect-prices が `[KANABELL] 価格検索レスポンス: HTTP 200` を多数出力、直近7日で `ESホストURL構築失敗` は販売cron・買取cron・Webのいずれにも0件）＝本番の環境変数は2分割形式。`split(":")[-1]` にすれば両形式に対応できる。URL構築失敗時に無言で0件になる設計も見直す価値あり
- [ ] **`_is_exactly_bracketed` が「パターン一致の隣」を見ている（潜在）** — 併記注記の carve-out 判定が、カード名の隣ではなくパターン一致範囲の隣の文字を見るため、`「A」細胞培養装置` `召喚魔術－「剣」` のように正規化後に区切り記号で始まる/終わるカード名77件では carve-out が効かない。実店舗データでこの carve-out が使われているのは `BLUE EYES WHITE DRAGON(青眼の白龍)` の2行だけで**実害0**。修正案（reviewer検証済み）: 両端から `_BOUNDARY_SKIP_CHARS` を読み飛ばしてから丸括弧を判定する。実測で本来用途テンプレ 15,237→15,495・注記欄テンプレは0のまま・実データ増減0（2026-08-04 reviewer指摘）
- [ ] **カードラッシュ販売「融合」が検索枠500件に本体を含まない** — 検索語「融合」は全融合モンスターに当たるため、5ページ100件×5の枠に本体商品が1件も入らない。2026-08-04の境界修正で汚染データ（別カード500件）が消えた結果、当該経路の取得が0件になった。検索語の絞り込みかページ送り上限の見直しが必要
- [ ] **NFKCで正規化されないダッシュ類が区切り集合に無い** — `― ‐ – — −` は `_FLEX_SEP_CHARS` にも `_BOUNDARY_SKIP_CHARS` にも入っていない。店舗がこれらを使うと新実装は「別カード名の続き」として弾く。実データ・マスタとも出現0件のため実害は未確認だが、カーナベル以外にダッシュ正規化の防御が無いことは記録しておく（2026-08-04 reviewer指摘7）
- [ ] **フランス語版プロモの扱い（要判断）** — 2026-08-04の境界修正で「(未開封)聖なるバリアミラーフォース/ForcedeMiroi【シークレット】{2025-FRP02}」（カードラッシュ販売・買取）が除外されるようになった。`/` を区切り扱いにした副作用で、同一カードの日仏名併記を「別カード名の続き」と判定している。同じ `/` の判定は「／バスター」46件を正しく弾いており全体では正だが、日仏併記だけは取りこぼしになる。フランス語版を同一カードとして扱うべきかを含めて判断する
- [ ] **トークン商品が本体カードの価格に混入する（既存挙動）** — 「トークン(増殖するG)」等がカード本体の検索結果に含まれる。SUPPLY_KEYWORDS に「トークン」が無いため。旧実装から同じ挙動で今回の変更起因ではないが、トークンは安価なため min_price を押し下げている可能性がある。除外すべきか、トークンも商品として妥当かは要判断（2026-08-03 第2弾の検証中に発見）

- [ ] **フェーズ3切替日の目視監視（2026-07-11〜12）** — min_price の意味変更（通常品最安化）初日は系列が上方シフトしうる。7/11・7/12 の /api/movers 上位とX投稿候補（JST20:07）に状態シフト由来の偽急騰が混ざっていないか確認。collection_runs への初回記録（7/11 JST5:00/7:00）も確認
- [ ] **買取収集復旧の実地確認** — 2026-07-11朝（JST7:00台）の tcg-collect-buyback 実行ログで「収集対象」が300件前後に回復しているか確認する。翌7/12はさらに増えるはず（未登録hot 215枚が7/11実行のupsertで追加されるため）。販売側 tcg-collect-prices（JST5:00）の「新規追加」ログも併せて確認
- [ ] **X再開後の初回投稿確認＋撤退ゲート判定（2026-09-15頃）** — ⚠2026-09-02訂正: 実態は post-x.yml の schedule がコメントアウトのまま（下の「放置注意」項が正）。再開したらこの項を有効化する — （旧記述）schedule再開済み（毎日JST20:07）。初回自動実行のログと投稿内容を確認。ゲート: bigmoveインプ中央値100未満かつGA4でX流入なしなら凍結
- [ ] **値動きランキング2系統の残り（featured系）** — post_big_movers はRPC一本化済み（2026-07-10）。featured/post_daily_movers のPython集計（get_price_movers Python版）が残存。featured の allowed_names はRPCの top_n に乗らないため完全一本化は不可。X実験の結果次第で系統ごと消える可能性があるため実験後に判断
- [ ] **稼働店舗2店未満の日のランキング無音** — ガード②の構造的制約（該当日は販売ランキングが空になる）。6店体制では実害小。店舗縮小時に問題化したら「データ不足」表示等を検討

## 完了

- [x] 2026-08-11 **★P1カタログ巡回の初回実走監視（8/10・8/11朝の2晩）完了・全項目健全** — カードラボ ok=2,439/2,440枚（従来893〜1,908）・error 0・14カテゴリ全て total==seen で verified・ページ失敗0・差分警告0。**403ブロック（⛔）は2晩で発生ゼロ**（カードラッシュも error 4→0）＝H1（ocnk共有カウンタ仮説）と整合。実行時間192/195分（カードラッシュ全快と相殺・予算内）。movers上位にカードラボ単独の急落なし。残=item_count乖離許容幅の校正（2週間後・バックログ既載）
- [x] 2026-08-07 **監査F9: カード詳細グラフを店舗平均→各日の店舗最安値へ統一** — 同一画面の最安値表示・movers・見積もりと統計量を揃えた（タイトルも「レアリティ別の最安値」へ）。詳細 decisions.md 2026-08-07
- [x] 2026-08-07 **監査F10: 夜間収集403の実測完了＋カードラボ販売 `&num=120` 化** — 効果測定を完了（案A=Session共有はブロック開始位置を動かせず・時間だけ201→175分）。カードラボ販売を120件/ページ＋item_count第一の終端判定へ（実機実走: ブラック・マジシャン256件が5→3リクエスト・照合通過47件は第6弾と同数）。カードラボ買取empty 67%は店の買取品目が狭いだけと特定しクローズ。**新事実: カードラッシュも8/5から同型403**（構造対策タスクを新設・裁定待ち）。詳細 decisions.md 2026-08-07
- [x] 2026-08-05 **ニューロン連携をcid方式へ根治（/card/by-cid 新設・6f014f4 デプロイ済）** — 拡張機能が読み仮名を検索してしまうバグの根治。原因=ニューロンの読み仮名は `<rt>` でなく `span.ruby` で、旧 content.js の除去が空振り。対処=名前スクレイピングをやめURLの cid（=konami_id、11ペア実測突合で同一体系確認）で連携。新モジュール neuron_link.py＋拡張v1.2.0（`?name=`併送フォールバック付き）。pytest 425件全緑・本番E2E 3系統確認。詳細 decisions.md 2026-08-05
- [x] 2026-08-05 **買い物指示書UI（複数店分割プランの内訳）** — 1行ヒントの価値検証がその日のうちに完了（ユーザーの実リストで¥1,000の差額を確認）したため本実装へ。ヒント行クリックで「どのカードをどの店で買うか」が開く: 店舗ごとにカード行（枚数×単価・レアリティ・商品検索リンク）＋小計＋送料＋「開く」リンク、末尾に送料込み総額。開閉状態は閉包フラグで保持しSSE再描画でも閉じ戻らない。ロジックは wish-split.js のまま変更なし（レンダラー `_wishSplitDetailHtml` を index.html に追加、既存 `_wishShopDetailHtml` と同じ流儀）。検証: 実機スタブ3種4枚で最適解（2店分割¥3,080・全1店案3通りより安い）と開閉・リンク先を確認／pytest 414件全緑／node 総当たり300件一致は不変。詳細 decisions.md 2026-08-05
- [x] 2026-08-05 **複数店分割の最安ヒント（1行）＋照合キー既存バグ根治** — 「組合せ最適化はしない」決定を部分上書きし、計算だけ行って1行ヒントを出すライト版を実装（内訳UIは価値検証後に判断）。`static/wish-split.js` 新設（店舗組合せ64通り全列挙＋局所改善。最低注文金額の違反はペナルティスコアで同ループ修復）。node の総当たり突き合わせで**ランダム300件全件が厳密最適一致**・表示額は実行可能プランなので過大表示にならない。**併せて既存バグ根治**: `/api/wish-shop-totals` の items が採用レアリティしか持たず、**レアリティ未指定エントリはDBに価格があっても見積もりが毎回リアルタイム検索へ落ちていた**。items に `rarity_pref` を追加し照合2箇所を統一。検証: pytest 414件全緑／実機スタブで3ケース（分割得¥1,300安・1店舗で揃わない・分割不要）確認。詳細 decisions.md 2026-08-05
- [x] 2026-08-05 **まとめ買い最安店舗に送料と最低注文金額を反映** — 商品代金の合計だけで並べていたため、①店舗ごとに違う送料（購入総額で無料になる店舗あり）②最低注文金額（**遊々亭1,000円・カーナベル2,000円で、未満は注文自体ができない**）を無視していた。とくに遊々亭は送料が常に無料なので、少額のリストでは「¥0で1位なのに実際は買えない」状態だった。新規モジュール `shipping.py` に6店のルールを出典URL・確認日つきの定数で持ち（手動管理・定期スクレイプしない）、`/api/wish-shop-totals` に `shipping`／`total_with_shipping`／`free_shipping_at`／`min_order_short`／`orderable` を追加。**送料ルール自体もAPIで配る**（リアルタイム検索で合計が後から増え、無料しきい値をまたぐとフロント側でも再計算が要るため。Python/JS の二重管理を避ける）。並び順は「揃う枚数 → 注文できる店舗を優先 → 送料込み合計」。注文不可の店舗は除外せず淡色＋不足額表示で下げる。**複数店に分けて買う組合せ最適化はしない**（決定）。検証: pytest 414件全緑（test_shipping.py 25件新設・店舗名が DEFAULT_SHOPS と一致することも固定）／ローカル実機で¥800の遊々亭が4位に落ち¥1,190のカードラボが1位になることを確認。詳細 decisions.md 2026-08-05
- [x] 2026-08-04 **is_target_card の前方/後方境界チェックを区切り集合と同期（既存バグ）**（07ee3d7・デプロイ済 JST21:22） — 末尾判定の6文字ハードコード `nc in "・－-ー、,&"` を廃止し、区切りの連なりを読み飛ばして次の文字を見る `_name_continues_after` に統一（読み飛ばし集合は `_FLEX_SEP_CHARS` から生成）。併せて前方の厳密括弧 carve-out を丸括弧だけに絞り、reviewer監査が検出した **カード名「融合」が全融合モンスターに一致（506件中500件が別カード）** も解消。マスタ15,522件総当たりで過剰一致 174→128件（解消46・新規0）・取りこぼし0件、実店舗11経路9,304ペアで LOST 769件（全て別カードかグッズ）・GAINED 0件。reviewer監査2巡。**本番確認**: /api/search?q=融合 が102件全て本物の融合（カーナベル60・遊々亭34・まんぞく屋5・トレコロ3）で、種別欄経由の別カード混入は消滅。詳細 decisions.md 2026-08-04

- [x] 2026-08-03 **他TCG商品の混入を根治（トレコロ／カードラボ／カーナベル）** — 複数TCGを扱う店がカード名の一致した別ゲームの商品を遊戯王の価格系列に混ぜていた問題を、**店側が持つゲーム種別の情報**で根治。①トレコロ＝検索URLに `&ct2=1010`（販売）／`&ct2=2010`（買取）②カードラボ＝商品名の**最初の `【…】` タグが `【遊戯】`**（`《未開封》【遊戯】…` があるので先頭一致は不可）③カーナベル＝`rarity_abbreviation` の `ラッシュ`／`ステンレス` を除外。**却下した案**: レアリティ名BL（共通名の混入を落とせない。`宿命の決闘` はデュエマ側と商品名が完全一致）／カテゴリ情報（トレコロのHTMLにゲーム種別なし、カードラボの main_category は遊戯王だけで672=シンクロ・676=効果…と分割）／型番判定（`-JP` WLは**遊戯王の14%を巻き込む**＝8桁パスワードと旧型番 `G6-B1`・`MA-51SR`・`DL5-090R`）。**実測（実装コードでの新旧比較）**: トレコロ販売84カード 679→723件（消失52＝全て他TCG・遊戯王の消失0）／トレコロ買取40カード 303→390（消失3＝全て他TCG）／カードラボ販売 308→304（消失4）／カードラボ買取 92→91（消失1）。**増加分は遊戯王の取りこぼし解消**（他TCGが5ページの枠を食っていた。青眼の白龍 販売60→90・買取28→57）。**落とし穴**: 買取に販売用の `ct2=1010` を使うと303→**0件**になる／URLの既存 `category=` は別軸で1010を入れると全カード0件。既存の他TCG 669行を price_history から削除（買取のステンレス14行は残す判断）。pytest 368件全緑。詳細 docs/decisions.md 2026-08-03
- [x] 2026-08-03 **値動きRPCにガード③（正体不明レアリティの除外）を追加**（本番適用済） — `aggregations`/`notify` の代表レアリティ選定は `(不明)`（P3の抽出失敗ラベル）と空文字を除外していたが、`get_price_movers`/`get_buyback_movers` は生の rarity でグループ化しており、**別々の正体不明品どうしを同一物として突き合わせていた**（偽陽性側）。両RPCの `shop_daily` に `AND COALESCE(rarity,'') NOT IN ('(不明)','')` を追加。買取側は空文字の書き込みが2026-08-03時点でも継続中（カードラボ・トレコロCB）でガード②も無いため、より危険度が高かった。**適用前後で本番RPCの出力は完全一致**（販売2件・買取11件が同値。純粋に将来の偽陽性経路を塞ぐ変更で、販売43系列・買取2系列が候補から外れる）。検証: 適用後の関数定義にガード③が入りガード②が販売側にのみ残ることをpg_procで確認／閾値1・上位500件・per_card5でも `(不明)`/空文字は0件。**なお当日は該当する値動きが無かったため「除去された行」の形での実証はできていない**（根拠は定義への反映と候補プールの縮小）
- [x] 2026-08-03 **レアリティ分裂・判断保留分の決着（`-`の根治／パラレル仕様の統合／KC細分化）** — ①**`-`（119行・増加中）の正体はレアリティ抽出の失敗ではなかった**。カードラッシュは単品カードに必ず `【レアリティ】` を付け、`【-】` は「単品カードではない」の印（ストラクチャーデッキ／ストレージ／未開封サイコロ／アクリルスタンド／金属製カード）。`_parse_cardrush_items` でこれらを捨てる根治を実施。実測（カードラッシュ58カード・照合通過2,467件）で `【-】` は14件・全て封入商品/グッズ・型番も `{-}`・**単品の巻き込み0件**、カテゴリも《未開封BOX》《その他》《アクリルスタンド》のみでレアリティ付き出品は存在しなかった。なお `未開封` をキーワード除外する案は**却下**（52件中47件が「(未開封)青眼の白龍」等の単品カードで巻き込む）。カードラボの `-` はトークンなので除外はせず、`normalize_rarity` でダッシュ1文字を `(不明)` へ隔離（トークンの可否判断は別タスクのまま）②トレコロCBの `ノーマル(パラレル仕様)`（2,567行）を**暫定で** `ノーマルパラレル` と同一物扱い（価格帯が他店と整合。別物と判明したら分けること）③KC系を細分化。`KC-R`→`KCレア`、`KC-N`→`KCノーマル` を新設し、判別できない生の `KC` は従来どおり canonical `KC` へ寄せる運用。検証: pytest 350件全緑／実スクレイプで炎王の急襲 7→3件（封入商品4件が消滅）・6カード×2店で `-` 出品ゼロ
- [x] 2026-08-03 **レアリティ系列分裂の根治（★ミレニアム系ほか計11系列・偽陰性）** — 同一レアリティを店舗が別表記で書き、rarity.py の aliases にどれも無いため生のまま別系列で蓄積されていた。**ミレニアム系は報告の2系列ではなく3系列**（トレコロCB＝「〜レア」あり／カードラッシュ・カードラボ＝なし／遊々亭＝略号 M-UR/M-SR/M-SE）。canonical を8件新設（ミレニアム4種・EXシークレットパラレル・KCウルトラ・シークレットレッド・ノーマルレア）、カーナベルの「オバフレ〜」3種を既存OF系の alias に追加、`normalize_rarity` に空白・丸括弧の前処理を導入（「〜レア」の自動除去はしない＝ノーマルレア/字レア/ベリーレアが壊れるため）。**「ノーマルレア」→「ノーマル」の既存 alias は誤りと判明**し解除（トレコロCBが同一店で「ノーマル」と「ノーマルレア」を併記しており、従来は同じ系列に混ざっていた＝偽陽性の温床。実測: 脆刃の剣 遊々亭NR ¥120 / ノーマル ¥50）。読み出し側の未正規化2経路（app.py のデッキ相場・notify の代表レアリティ選定）にも normalize を追加。**既存 price_history は触らない**（値動きRPCは最新2日しか見ないため1〜2回の収集で復旧、グラフは読み側正規化で即時統合。フェーズ3 A1 と整合）。検証: pytest 321件全緑／DB実データ88表記を新旧の実装に通し系列 81→64・**修正前に認識できていた値の正準名は1つも変化なし**／RPC本体のSQLで共通2店以上 7,673→7,720（**47系列が新たにガード②の対象に**: OFプリシク10・ミレニアムスーパー9・KCウルトラ7・ミレニアムウルトラ6・OFウルトラ6・EXシークレットパラレル4 等）／**切替日に新規出現する値動き0件**（ガード①が表記変更店を構造的に除外するため。X再開目安 8/7 は据え置き可）／カード詳細の系列数は 命削りの宝札 7→5・死者蘇生 18→16・赤き竜 14→12。詳細 decisions.md 2026-08-03
- [x] 2026-08-03 **ページ送り欠如の解消 第6弾（カードラボ販売・カードラッシュ買取）** — 1ページ目しか取っていなかった残り2経路に `max_pages=5` のページ送りを実装。終端検出は店舗ごとに別（カードラボ＝範囲外で1ページ目が返るためページャ`page=N`最大値＋60件未満＋内容重複／カードラッシュ買取＝`lastPage`）。実装コードそのもので新旧比較し**欠落0件**（販売239→261件・買取300→337件）、ランダム60件での**追加リクエスト0件**、高額カード40件＋買取高額20件で**新規取得0件＝見かけの急落・偽値上がりなし**（X再開日は据え置き可）。第4弾で `Re：EX` が1ページ上限に接触していた件もこれで解消。詳細 decisions.md 2026-08-03。**問題なしと確認済みの経路**: 遊々亭（販売・買取とも2ページ目なし）、まんぞく屋（disp_number=100で全件）、カードラボ買取（2ページ目=1ページ目）、トレコロ（実装済み）、カードラッシュ販売（第2弾で実装）
- [x] 2026-08-03 **検索語正規化の不揃いを是正 第4弾（空白圧縮＋コロン除去の共通化）** — `_squeeze_spaces()` を新設し `_normalize_search_query` / `_cardrush_search_query` の両方で使用。コロンはカードラッシュ専用ではなく共通側で落とす形に統一。実測: 空白圧縮は影響101件×5経路でカードラボ販売 124→133件・**減少0**、コロン除去は影響9件×7経路で**カードラッシュ買取 0→27件・遊々亭買取 6→29件・遊々亭販売 7→16件**・カードラボ買取 17→18件・**減少0**。トレコロは対象外（全15,522件で余分な空白が発生しないことを確認）。検索語生成の棚卸しで残るのは駿河屋のみ。詳細 decisions.md 2026-08-03
- [x] 2026-08-03 **検索キャッシュを店舗束形式へ 第3弾（キャッシュ汚染の根治）**（cba8a22・デプロイ済） — キー=カード名のみで店舗集合を持たないため、店舗を絞った検索が全体を上書きし15分間ほかの店舗が「0件 (cached)」になっていた。店舗ごとの独立TTL・partial印へ変更し、取得失敗店舗は非キャッシュに。詳細 decisions.md 2026-08-03
- [x] 2026-08-03 **店舗検索の取りこぼし修正 第2弾（カードラッシュ）＋ページ送り追加**（bdfc702・デプロイ済） — 照合の区切り集合を `_FLEX_SEP_CHARS` に集約し実測で仕分けた記号を追加（ギリシャ文字等の識別子は文字のまま）。カードラッシュ専用の検索語生成を新設。**販売にページ送りを実装**（検索語を広げた副作用で1ページ100件の枠から正解が押し出される回帰をreviewerが検出）。カード名が区切り記号だけの場合の空パターンガードも追加。効果=販売301→422件・買取187→220件、他8経路は増減ゼロ。検証=pytest 210件全緑／実装コードでの総当たり8,301件×15,423件で取りこぼし0件・過剰一致0件／実商品名1,356件で混入0件。詳細 decisions.md 2026-08-03
- [x] 2026-08-03 **店舗検索の取りこぼし修正 第1弾（トレコロCB・カーナベル）**（1133605・デプロイ済） — トレコロは検索語を「ダッシュ→U+FF0D統一」のみに変更（旧実装のスペース置換が原因だった）。カーナベルはESの半角/全角混在格納に対し全角版wildcardを併投（全角化は英数字のみ。記号も全角化すると混在表記2種類が0件のまま）。販売・買取・画像取得の3箇所に適用。あわせて**X自動投稿を一時停止**（新規取得の安値で偽の急落が投稿されるため。高額32件中11件が閾値超過を実測）
- [x] 2026-07-11 **フェーズ3（生成側の根本治療）フル構成 完了** — P5観測店舗記録（collection_runs）/P6a誤補正停止/P1名寄せ＋tracked_cardsバックフィル（統合65G・改名58行・再ドライラン0件）/P3 rarity"(不明)"隔離/P4状態2値（min_price=通常品最安・min_price_any新列）＋P2 code列/P6b deck経路ignore_duplicates。DDL2本適用済み・pytest166件全緑・reviewer3巡。詳細 decisions.md 2026-07-11
- [x] 2026-07-10 **X自動投稿を再開（閾値±500円/±30%に再校正、候補A採用）** — ガード後60日実測で校正、テスト境界値追従、schedule復活。撤退ゲート2026-09-15頃
- [x] 2026-07-10 **プライバシーポリシーをGA4実態に追従** — 既存モーダルの「アクセス解析ツールを使用していません」を GA4利用の明示＋オプトアウト案内に更新。Web Push・フィードバックの項を新設、改定日追記
- [x] 2026-07-10 **パックリストの表示用/収集用を分離（include_emptyフラグ）** — 発売前パックがサイトから消える副作用を解消、キャッシュキーも用途別分離
- [x] 2026-07-10 **フェーズ3設計文書を作成** — docs/design-phase3-generation-side.md（名寄せの真の発生源=tracked_cards登録経路と判明、code横断キー不可の確認含む）
- [x] 2026-07-10 **値動き検出に2ガード導入（共通店舗＋複数店確認）＋Supabase適用・本番検証済み** — RPC店舗粒度化＋per_card引数・notify.py 2ガード＋基準日改良・x_poster RPC部分一本化・app.py days=3統一。60日リプレイで偽陽性52件全滅/本物1件生存（53/53期待一致）・reviewer監査2巡・pytest 98件全緑。SQLは管理API経由で適用し（旧3引数版DROP・4引数版のみ登録を確認）、本番RPC出力11行を生データ再計算と突き合わせ全件一致（全行が共通2店以上・同方向2店以上）
- [x] 2026-07-10 **騰落率パイプラインの前提監査（別セッション）完了** — 偽陽性率98%確定（53件中本物1件。店舗欠測26/在庫入替25/系列分裂1）。トレコロCB買取混入疑いは白（キズありSKU×0.8）。買取側は25日分で本物0件・在庫入替型ゼロ。詳細は decisions.md とメモリ

- [x] 2026-07-10 **買取収集激減（7/7以降 1,185→50行/日）の根治4点** — ①TCG PORTAL deck-guides のHTML改修（h3→p）に fetch_deck_cards を追従 ②tracked_cards 1,243件の PostgREST 1000行上限を分割取得（order付きrange）で根治＋ _insert_new_cards を upsert(ignore_duplicates) 化 ③Wikiカードリスト未掲載パックに get_pack_list の枠を消費させない ④sync_regulation の取得元を公式カードDB（forbidden_limited.action）へ移行。ドライラン検証: hot候補562件・収集対象347件（障害前206件を上回る）・規制197枚復活・テスト75件全緑・reviewer監査済（中指摘のorder付与を反映）
- [x] 2026-07-10 X投稿を大変動単発形式（post_big_movers）へ切替（閾値は60日実測校正・reviewer監査で繰り上げ不一致を検出し修正・テスト75件全緑）
- [x] 2026-07-10 GA4計測タグを導入し本番稼働開始（環境変数駆動・測定ID設定済み・本番HTMLでタグ出力を実機確認）

- [x] 2026-07-10 x_poster 投稿閾値を実データ校正（DAILY_MIN_DIFF 100→50円、60日8,854ペアの分布から導出。docs/decisions.md 参照）
- [x] 2026-07-10 柵5箇所の由来調査＋コメント追記（deck_image `_v2`/1KB閾値=8983439由来、scraper sleep(2)=初回アップロードから根拠なし、rarity「レアレア」=実データ非実在の投機的登録と確定、_EX_PROP_IDS=一次情報源の記録なし）
- [x] 2026-07-10 fetch_guard.ALLOWED_PATH_PREFIXES を削除（未使用の検証機構ごと除去、テスト追従）
- [x] 2026-07-10 README.md の失効記述更新（3店舗同時検索→販売7店舗・デフォルト6店、ファイル構成→docs/ARCHITECTURE.md誘導）
- [x] 2026-07-10 前提監査（assumptions-check）＋棚卸し（inventory-audit）実施
- [x] 2026-07-10 未使用3ファイル削除（c859e17）・ルート空フォルダ削除・失効文書6枚を `_archive\docs_2026-03\` へ移動
- [x] 2026-07-10 test_fetch_guard の失効期待値を追従（67640e8）
- [x] 2026-07-10 MAMB方式へ移行（状態ファイル新設・ARCHITECTURE.md全面改訂・CLAUDE.md改訂）

## ページ構成改善（UXサーベイ 2026-08-25）

- [x] 2026-08-25 **ページ構成の根拠調査を完遂** — TCGYM本番実測＋競合4サイト実測（TCGplayer/価格.com/eスターボックス/トレカプライス）＋設計指針4件（M3/Apple HIG/NN-g/Baymard）。レポート `docs/ux-survey-2026-08-25-report.md`、生データ `docs/ux-survey-2026-08-25-handover.md`
- [ ] **【要判断】C-1 レアリティ13種問題の捌き方** — (i)URL分割(TCGplayer/eスターボックス型) / (ii)ページ内グループ分け(トレカプライス型) / (iii)既定フィルタ(最小改修)。**ユーザー判断待ち**
- [ ] **【要判断】C-2 販売＋買取をカードページに併置するか**（eスターボックス型）／**C-3 タブ7個の扱い**（2段圧縮 or 4つに束ね直し or ボトムナビ）。**ユーザー判断待ち**
- [x] 2026-08-25 **第1弾（B-1〜B-4＋C-1(iii)）を本番デプロイ**（991cbee） — スマホ全高 34,215px→5,603px（本番実測）。店舗サマリーを価格昇順＋差額＋レアリティ統一／縦順入替＋グラフと効果テキストを既定折りたたみ／PC二重スクロール解消／既定20行＋もっと見る／レアリティ既定フィルタ＋バナー。reviewer監査14件を修正後、ローカルと本番の両方で実機検証（件数一致・同額タイの店舗一致・直リンク4パターン・A8ピクセル非再発火・チャート遅延描画）。pytest 692件全緑
- [ ] B-5 トップの空状態を埋める（**騰落だけに頼らない**。競合も当日差分は¥0だらけと実測）
- [ ] 計測: GA4でタブ切替・「もっと見る」・レアリティ切替を1〜2週（C-3bの判断材料が現状ゼロ）
- [x] **（2026-08-31 のタブ統合 5a57676 で解消済み。2026-09-02 総点検で確認: 該当行は現存せず esc() 済み）【XSS・今回の変更外】買取サマリーの未エスケープ** — `templates/index.html:3629-3631` の `<div class="slabel">${s}</div>` と `${shopBest.rarity||'-'}` が `esc()` を通っていない。`s` は固定の店舗名だが **`shopBest.rarity` はスクレイプ由来の文字列**で、店舗HTMLが汚染されれば注入経路になる。2026-08-25 のUI改修レビューで発見（過去の横断XSS対策の取りこぼし）
- [ ] シェア文（`_buildShareOptions`）がレアリティ絞り込みに追従しない — 絞り込み後にシェアすると画面と違う価格を投稿する。2026-08-25 の第1弾ではスコープ外とした
- [ ] **0件検索が `#results` を innerHTML ごと破壊する**（`templates/index.html` の `renderAll()` 0件分岐） — `#priceChart` / `#buyInline` の DOM とトグルリスナーが消え、次に正常なカードを検索しても**リロードするまで買取セクションと価格グラフが復活しない**。無言で機能が消えるため気づきにくい。0件分岐を専用要素の表示切替に変える。2026-08-25 C-2レビューで発見（既存バグ・新機能が継承）
- [ ] 買取の進捗行で `shop_error`（赤）が直後の `shop_done`（緑・0件）に上書きされ、失敗が緑で表示される（`app.py` が `shop_error` の後も同じ店舗に `shop_done` を送るため）。買取タブ・インライン両方に該当。失敗自体は注記で明示されるので軽微
- [ ] 買取タブの店舗トグルとインライン買取の店舗集合が食い違う（インラインは `shops` を送らず常に `DEFAULT_BUYBACK_SHOPS` 全5店。タブ側でユーザーが絞っていても無視される）
- [ ] **【データ品質・根本原因】店舗ごとに `code`（型番）の抽出品質が違う** — 2026-08-25 のC-2で型番を画面に出したことで露見。本番実測（増殖するG）:
  - まんぞく屋6/カードラボ16/遊々亭21 は**全件が正しい型番**、カードラッシュ21件も正常（弾いた1件 `アジアTTP1-JP079` はアジア版＝別商品なので弾くのが正しい）
  - **カーナベルは 0/10。`TTP1` `DBSW` のような弾の略号のみで、`SDモ さ` という壊れた値も混入**。略号は同じ弾の別カードと衝突するため、型番として使うと別カード同士を同一視する危険がある
  - **トレコロCBは 3/26。`LPST-JP007PSE` `20TH-JPC8220SE` のようにレアリティ接尾辞が付く**、または `23434538` の数値内部ID
  - 暫定対処として `_looksLikeCardCode()` で「型番らしい形」のみ表示・突合に使う**表示側ガード**を入れた（`templates/index.html`）。**根治はスクレイパー側**（カーナベルの型番抽出、トレコロの接尾辞分離）
  - **2026-09-01 追加調査（カーナベル）**: 「APIに型番が無い」は司令塔の**推測で、誤り**だった。**商品詳細ページのHTMLに `シリアル番号:MACR-JP036` の形で正しい型番が載っている**（`?act=sell_detail&genre=1&id=...`、`table.pc_sell-card-detail_ranks` 内。scriptタグ内のJSONではなくサーバ描画テキスト）。検索結果ページには出ない（この店の検索はES API駆動）
  - 実測（灰流うらら）: カーナベル144行のうち**他店からの補完で108行(75%)は既に型番あり**。残り36行が指す**商品ページはわずか9ページ**なので、1カードあたり最大9リクエストで埋まる
  - **【決着】2026-09-01: ES API の全87フィールドを実際に確認した結果、型番を持つフィールドは無い**（`KANABELL_CLOUD_ID` をユーザーが差し替えて検証）。最も近いのは `category3_abbr` だが、これは弾＋言語コードまで（例 `20TH-JPC`）でカード番号が無い。`id`/`main_card_id`/画像ファイル名は内部ID。**型番は詳細ページのHTMLにしか存在しない**
  - 副産物: `category3_abbr` の粒度は弾によって違う（`20TH-JPC` のように言語コードまで入る弾もあれば `TTP1` だけの弾もある）。現行の補完は**前方一致**で照合しているため、長い接頭辞のときは自動的に精度が上がっている
  - **【判断】ユーザー決定: 詳細ページの取得はやらない（2026-09-01）**。理由: 現状すでに75%が他店からの補完で埋まっており、残り25%のために初回27,000リクエスト（1カード最大9ページ×追跡3,000枚）を掛ける価値が薄い。カーナベルの商品IDは不変なので `商品ID→型番` の永続キャッシュにすれば以後はほぼ不要だが、それでも初回コストが見合わないと判断
  - もし将来やるなら: **価格推移ランキングに載る高額カード（最安¥1,000以上・約40枚）だけに限定**し、永続キャッシュ前提で夜間収集に少しずつ混ぜるのが現実的。カードラボの403（流量ベース遮断）とは別ホストなのでそちらへの影響は無い
  - 接尾辞の安全な剥がし方の案: 他店の正しい型番を既知集合として、トレコロの値が**その前方一致になっているか**で判定する（接尾辞一覧を推測せずに済む）。要検証
  - **C-1(i)（レアリティ別URL分割）を型番単位に広げるなら、この品質差の解消が前提条件になる**
- [ ] **非表示UIが挙動を決めている構造の是正** — 店舗トグル（`#shopToggles` / `#buyShopToggles` / `#deckShopToggles` / `#metaShopToggles`）は 2026-06-09 以降 `display:none` だが、検索時の `shops` パラメータはこの非表示DOMから組み立てられている。買取側が3店のまま放置され**5店中3店しか検索していなかった**（2026-08-25 発見・是正済み）。同種の事故を防ぐため、送信する店舗集合は**定数側（サーバの DEFAULT_*_SHOPS）を正とし、UIから組み立てない**形へ寄せる
- [ ] **【恒久対応】トップの価格推移をRPC（DB側集計）に寄せる** — `/api/top-movers` は現在 PostgREST 経由で1日分の `price_history` を全行取得している。**本番実測で最新日は30,851行**（3日分＝約9万行を1時間ごとに読む）。応急で複合キー安定化＋上限10万行にしたが、Render無料枠のメモリと転送量に対して本来重すぎる。既存 `get_price_movers` と同じくDB側集計のRPCを新設して寄せる。**代表レアリティ選定（(不明)/空を除外して最安）と共通店舗ガードをSQL側で行うこと**
- [ ] トップの `TOP_DECKS_LIMIT=8` / `TOP_DECKS_CACHE_SEC=1800` / `MOVERS_MIN_PRICE=1000` / `TOP_MOVERS_CACHE_SEC=3600` / `MOVERS_STABILITY_DAYS=1` は仮置き（`# TODO: calibrate from data` 付き）。実データとUXで校正する
- [ ] **sitemap.xml が `http://` を出している** — `app.py` の sitemap 生成が `request.url_root` 由来で `http://tcg-price-compare.onrender.com/...` を出力している（本番実測）。canonical/sitemap は https にすべき。2026-08-31 のタブ統合調査で発見
- [ ] `/api/price-history` はフロントからの参照が無くなった（価格推移グラフ削除に伴う）。ルートは残してあるので、削除するかは影響調査のうえ別途判断
- [ ] **全角/半角のカード名差異による名寄せ失敗の横断確認** — 2026-08-31 の型番補完実装中に発覚: **トレコロCBだけカード名を全角で返す**（`増殖するＧ` ／ 他店は `増殖するG`）。型番補完側は NFKC 正規化で対処したが、**同じ罠が他の名寄せ処理にも潜んでいる可能性**がある（`price_history` の card_name 突合、`tracked_cards` の登録、デッキ見積もりの照合など）。横断的に確認する
- [ ] `code_source`（型番が店由来か推測補完かの区別）は現在メモリ上の dict にのみ持ち、DBには保存していない。運用・デバッグで必要になったら `price_history` / `buyback_history` に列追加を検討（2026-08-31 判断: 表示・突合には使わないため今回は不要）
- [ ] **`get_top_movers` は被覆インデックス `idx_price_history_date_card_shop_rarity` に依存する** — このインデックスを消すと anon の statement_timeout(3s) を超えて 57014 で落ちる（実測: 索引なし3.4秒 / ありで844ms）。DB再構築時は `supabase_rpc_top_movers.sql` の末尾にあるインデックス作成も必ず適用すること
- [ ] **【要判断・放置注意】X の値動き投稿が2026-08-03から停止したまま** — `post-x.yml` の `schedule:` がコメントアウト。**再開条件（収集4回分）は 2026-08-07〜09 に満たされている**。再開はコメントを外すだけ。**撤退ゲート（当初2026-09-15頃）は投稿が出てから測り直すこと**（止まったまま「効果なし」と判定しない）。2026-09-01 にユーザー判断で再開は見送り
- [ ] **【将来】値動きの定義2つ（get_top_movers / get_price_movers）の統合** — パラメータ違いではなく設計が別物（比較する2日・レアリティの畳み方・¥10以下の除外・ガードの構成）。統合するには稼働中の `post_featured_movers` の出力が変わらないことを過去データで確認する必要がある。差分表と実測値は docs/decisions.md 2026-09-01
- [ ] **辞書に無いカード名の表記揺れ2件** — `K9-00号 ルプス` / `K9－00号 ルプス`、`炎舞-「天キ` / `炎舞－「天キ` は**どちらの表記も cardnames_ja.json に無く**、どちらが正式名称か判定できないため両方 active のまま残した（2026-09-01）。後者は名前が途中で切れており元データが壊れている疑い。辞書の更新か、公式DBでの確認が要る
- [ ] tracked_cards に表記揺れの重複が再び入らないか監視する仕組み — 2026-07-11 の名寄せ修正後も 07-19 まで半角表記の新規登録が続いていた形跡がある（登録経路のどこかが補正を通っていない可能性）。定期的に `normalize(card_name, NFKC)` で重複検出するチェックを入れるとよい
- [x] 2026-09-01 `/api/packs` が Windows ローカルで500になる問題を修正 — 「環境依存だから無害」ではなかった。Wikiの404を捕捉して警告 `print` する処理そのものが cp932 で落ち、捕捉済みエラーが未捕捉例外化して500になっていた。app.py 冒頭で stdout/stderr を UTF-8 に固定して根治
- [x] 2026-09-01 **「デッキビルドパック グロリアス・ヴィクターズ」の収録カードが0件** — 解決。Wikiにページは存在し（`デッキビルドパック −グロリアス・ヴィクターズ−`）、公式名には無いダッシュ(U+2212)でサブタイトルが囲まれていたため `_resolve_wiki_page()` が到達できていなかった。ダッシュで囲むバリアントを追加して44件取得。※当初「Wiki内検索でヒット0件」を根拠に「ページが存在しない」と結論したが、その検索コード自体が壊れており（既知の語で検索してもヒット0）、誤った断定だった。ユーザーがWikiのURLを提示して発覚
- [x] 2026-09-01 **カード名の閉じ括弧が落ちる抽出バグ** — 解決。`pack_scraper` の抽出正規表現が非貪欲だったため、名前の内側や末尾の閉じ括弧の手前で切れていた（`_fetch_from_wiki` と `fetch_theme_cards` の2箇所）。**変更前に実データで影響を計測**（パック23件1,423名＋テーマ7件645名＝2,068名）し、貪欲化で39件が閉じ括弧の復元のみ変化・取りこぼし0・取りすぎ0を確認してから適用。公式カード名辞書(data/cardnames_ja.json)との照合でも、切れた形は0/5一致・修正後は5/5一致（1件はNFKC正規化後）。既知だった `炎舞-「天キ` も同じ原因
- [x] **（2026-09-02 b00d265 で date.today() 相対に修正済み）テストが日付で落ちる** — `tests/test_admin_unreleased_release_date_guard.py` の3件が `release_date` を`2026-09-01` 固定でハードコードしており、日付が進むと「発売日が過去」判定に入って失敗する。2026-09-02 時点で3 failed。テストスイートが常時赤になるため、相対日付（`今日+N日`）に直すべき。2026-09-02 発見
- [ ] **移行用バックアップテーブルの削除** — `watched_pages_bak_20260902` / `unreleased_cards_srcurl_bak_20260902`。2026-09-02のyu-gi-oh.jpリニューアル対応のDB移行前に取得したもの。次回 watch_unreleased.py を実走して問題が無いことを確認できたら削除してよい

## 2026-09-02 定期総点検由来（台帳: docs/audit-2026-09-02.md）

### 第2バッチで残した記録（レビュー班指摘・未対応）
- [ ] **セレクタ破損の代替監視** — 0件≠失敗の修正（第2バッチ）で「例外なしの0件」はアラート対象外になった。`collection_runs.shop_stats` の店別 empty 率が閾値超（他店は ok）を日次レポートに出す（台帳 C-M8/C-M10 と同じ束で）
- [ ] **相場RPCの work_mem=64MB は実測35MBからの余裕値** — 並べ替えノードが2段あるため最大2×64MB。`EXPLAIN (ANALYZE)` で quicksort（メモリ内）を確認済み（2026-09-02）。Supabase のメモリ余裕が不明なため、プラン確認後に必要なら 48MB へ
- [ ] **相場RPCの残リスク: 索引スキャンがコールド時に約1.7秒** — work_mem 対策後も cold cache で合計2.4秒（anon の statement_timeout 3秒に近い）。再起動直後の1回目失敗は残りうる（再試行で回復）。恒久策は日次集計テーブル化か authenticated ロール（8秒）での呼び出し
- [ ] **json 経路が本番未適用の環境で10分ごとに WARN が出る** — featured_pack と同じ「未適用と判定したら記憶」に揃える（本番は 2026-09-02 適用済みのため現状は出ない）
- [ ] **検索の連打で 429 が「エラーが発生しました」と出る** — 世代管理（b00d265）後、3秒以内の連続検索はサーバの制限で429→汎用エラー文言。「少し待って再検索」の文言に分ける
- [ ] **test_ygores_sync.py はリポジトリ直下で CI 対象外だった** — 第2バッチで tests.yml に追加（要確認）。将来 `tests/` へ移動
- [x] **（2026-09-02 完了: 重複2,664行を削除→UNIQUE作成→upsert化 ce600f2）買取履歴の UNIQUE 化と upsert 化**
- [x] **（2026-09-02 ユーザー操作で変更済み・Render で確認）買取cronの時刻変更** — `0 0 * * *`（JST9:00）。販売cronとの重なり25分を解消
- [x] **（2026-09-02 実行済み: 14,241件・新カード178枚。名前索引も同期ジョブで更新、Web再起動で画像欠け解消を確認）YGOResources 一括投入の実行** — `import-ygores.yml` を手動実行して公開ミラー（9/1更新）から追いつく。sync の再開カーソル方式（第2バッチ）だけでも約4回（24時間）で完走見込み

### Supabase 無料枠（500MB）超過への対応（裁定待ち・2026-09-02 実測）
- DB 1,095MB のうち price_history が 1,006MB（本体289MB・索引717MB＝7割が索引。文字列キーを3本の索引に重複保持）。35日より古い行を読む機能は現存しない（/api/price-history のみ90日読むが画面から未参照）
- 案1: 保持90→31〜35日＋冗長索引2〜3本削除＋VACUUM FULL → 490〜550MB（余裕なし・過去データ喪失）
- 案2【推奨】: カード名/店名/レアリティを整数キー化（互換ビューで読み取り側を吸収、書き込みは price_persist に一本化済み） → 90日保持のまま約250MB
- 案3: Pro（$25/月）
- **完了（2026-09-02）: 案2で実施し §1〜§6 すべて適用。DB 1,095MB → 433MB（無料枠500MB内）。** 書き込み経路は本番検索と新弾収集ジョブ（328行）で確認。次回の定期点検で Supabase ダッシュボードの Database size と成長率（毎晩約+8MB・90日で安定）を確認

### 第3バッチ候補（台帳 §7-3）
- [ ] レート制限の穴3群: `/api/meta/deck`（`?force=1` 公開）／DB直叩き6本／`/api/card-image-proxy`（台帳 B-2・B-7・B-8）
- [ ] 収集バッチの堅牢化: `update_cardnames.py` の下限ガード（1000→現行×0.95）／`save_pack_list` の原子性／`main()` の try/finally で記録・通知を必ず出す（台帳 C-H1〜H3）
- [ ] 買取収集の Discord 日次レポート追加／日次レポートに注目・周辺の内訳と上流取得可否（台帳 C-M1・C-M8）
- [ ] スマホ表示: 最新弾ページのラジオ配置／検索結果の店舗サマリーの縦幅（台帳 U-3・U-5。U-1 は別セッションが 75f83e2 で対応済み）
- [ ] `trending_scraper` の検証不能時「全通し」を安全側へ（台帳 C-H5・要裏取り: tracked_cards のうち辞書に無い名前の列挙）
- [ ] `collect_x_metrics` の無限再試行（96回/日）／`last_collected_at` の `except: pass`／ヘルスチェック未登録3店（台帳 C-M5・M6・M10）
- [ ] フロント: packs.js 一括検索の行ID連番／ランキングのタブ連打／買取ランキングのエラー無言／モーダルの Escape／sw.js の画像無期限キャッシュ（台帳 F-3〜F-7）
- [ ] 死んだ仕組みの整理: `test-x-post.yml`（旧形式で本番Xに実投稿しうる）削除／`applyMetaDeck`・`PRESETS`・未参照CSS（台帳 §2・§4）
