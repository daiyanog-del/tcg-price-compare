services:
  web:
    build: .
    ports:
      - "5000:5000"
    env_file:
      - .env
    environment:
      - FLASK_DEBUG=0
      - PORT=5000
    restart: unless-stopped
    volumes:
      - cache_data:/app/.cache

volumes:
  cache_data:
