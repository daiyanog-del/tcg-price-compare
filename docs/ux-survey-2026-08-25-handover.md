# TCGYM ページ構成改善調査 — 引き継ぎ書（2026-08-25）

> 旧セッションがクラッシュを繰り返したため、途中成果をここに固定して新セッションへ引き継ぐ。
> 発端: ユーザーの依頼「タブでスイッチする構成や見やすさが最適か気になる。改善案を出してほしい」
> → 初回提案（案A/B/C）に対しユーザーから「根拠は？」→「どう直すかは調査した方がいい。ただし同種サイトが完璧とも限らないし、縛られてほしくない」との方針指示。
> **調査の姿勢: 他サイトは「正解」ではなく「判断材料」。TCGYMの前提（6店舗横断比較・デッキ起点・日本語）と違う点は明示して採否を判断する。**

## 1. 完了済み: TCGYM本番の実測（一次情報・信頼してよい）

本番 = https://tcg-price-compare.onrender.com （ブラウザでPC幅800pxとスマホ幅375pxを計測。スクリーンショットは環境制約で取れず、位置・高さの数値計測のみ）

- トップ（スマホ375px）: ヘッダー135px＋モードタブ**3段**144px → 検索窓が y=293px から。3段目は「一人回し」だけ全幅で孤立
- トップの空状態: 「カード名を入力して検索」1行のみ。/api/movers・/api/trending とも当日 items:[]（空）
- 検索結果（スマホ・灰流うらら）: 全高 **34,215px**。順序 = ヒーロー(カード画像+効果テキスト338px) → 価格グラフ(286px) → 店舗サマリー6枚(765px) → 並べ替え行(94px) → レアリティフィルタ14ボタン(252px) → 一覧184行×165px。**一覧先頭が2,248px目（約6画面下）**
- 検索結果（PC800px）: 一覧まで1,439px。一覧は max-height:324px の内部スクロール（ページスクロールと二重）
- 店舗サマリー: 並びがサーバー返却順（index.html:2072付近 `Object.entries(d.by_shop)`）で**価格順でない**。最安¥180が2枚目、先頭は¥300。ノーマルとウルトラが混在し店同士の比較になっていない
- 各タブの空状態（スマホ）: マイデッキ=取り込み3手段を全展開870px／購入候補=2行／最新弾=4ボタン／環境デッキ=17ボタン（唯一充実）／買取=騰落10件
- 「一人回し」だけ別ページへのリンク。solitaire側ナビは「販売価格/一人回し」のみで他5タブが見えない（非対称）
- 副題がタブごとに変わる遊び（「資金繰りは、大事だよね。」等）。ヘッダー135pxの一因
- タブ実装: `.mode-tabs`（index.html:809-816）、switchMode()（同:2447）、URLハッシュで復元（#buyback等）

## 2. 完了済み: 初回提案（案A/B/C の骨子・根拠補強前のもの）

- **案A（小・現構成のまま）**: A1 検索結果の順序入替（サマリー→一覧を先、グラフ・効果テキストは折りたたみ）／A2 店舗サマリーを価格順＋レアリティ統一（または#rgridのレアリティ別を昇格）／A3 一覧の既定を在庫あり+選択レアリティ+20行「もっと見る」／A4 スマホタブ2段以内（一人回しをヘッダー/フッターへ）／A5 スマホヘッダー縮小135→80px／A6 空状態の手当て（騰落0件の日はTier1や最新弾を出す）／A7 マイデッキ取り込み説明を閉じる
- **案B（中・タブを目的で束ね直す）**: 「価格（販売⇄買取はセグメント切替・最新弾サブ）／デッキ（環境デッキサブ）／購入候補／一人回し」の4つに。弱点=3群の分け方は主観・利用データなし
- **案C（スマホは下固定ボトムナビ）**: 常時見える・親指で届く。Bと併用可
- 推奨順: A1-A3先行 → タブ切替のGA4計測1-2週 → B/C判断

## 3. 完了済み: 設計指針の原典確認（エージェント調査・完了報告受領済み）

- **Material Design 3 Navigation bar**: 「3〜5個」を原典で明記（m3.material.io/components/navigation-bar/guidelines・確認済み）。超過時はDrawer等を案内。**TCGYMの7個は推奨枠超過**
- **Apple HIG Tab bars**: 現行原典に「iPhoneは5個まで」の数値は**無い**（巷説）。あるのは「少ない方がよい」「オーバーフロー回避」の原則。iPadOS側にのみ「デフォルト5個以下」の記述（developer.apple.com/design/human-interface-guidelines/tab-bars・プロキシ経由で確認済み）
- **NN/g 空状態**: ①状態を明示 ②使い方をその場で教える ③次のアクションへ直接導線（nngroup.com/articles/empty-state-interface-design/・確認済み）
- **thumb zone（親指ゾーン）**: 一次資料（Hoober, UXmatters 2017）は**401で未確認**。二次（Smashing Magazine 2016）で大枠のみ確認。約75%が親指操作・画面下部が届きやすい
- **NN/g 比較表**: 「差がある属性を目立たせる/上へ」「同一属性は列結合」（explicit-differences）。**価格順ソートの明示推奨は本文に無し**
- **Baymard**: デフォルトソートは「多様性ベースの関連性」推奨（baymard.com/blog/default-sort-type・確認済み）。load more vs pagination vs 無限スクロールの記事本文は**未確認**（取得失敗。二次情報では「load moreボタン+遅延読み込みが好ましい」論調）

## 4. 完了済み: TCGplayer 実地計測（スマホ幅・私のブラウザで確認済み）

- 検索段階で「**収録弾×レアリティごとに1商品**」に分割され、各カードに「12 listings from $0.95」+ Market Price を表示。**レアリティ選択が先・出品一覧が後**（TCGYMは436件を1画面平積み → 対照的）
- 商品ページは1画面目に「8 Listings / As low as $2.70」の答え。効果テキストやグラフは前に無い
- 出品一覧の既定ソート「**Price + Shipping（送料込み）**」・10件/ページ・状態(Near Mint等)と出品者評価を各行に表示
- 絞り込み（Printing/Condition/Language）は「Filters」に折りたたみ

## 5. 未完了の作業（新セッションでやること）

1. **Cardmarket**: Cloudflareのbot検証で足止め（「しばらくお待ちください」画面まで確認）。ブラウザペインを表示状態にして手動で通過を待つか、諦めて「取得不可」と記録
2. **価格.com**: 未着手。スマホ幅で ①トップのナビ構成 ②商品ページの上から順（最安値→店舗一覧の順のはず）③店舗一覧の既定ソート を計測
3. **国内TCG相場サイトの洗い出し**: エージェント2回中断で**成果未回収**。部分成果が C:\Users\daiya\AppData\Local\Temp\claude\C--Users-daiya\22ca663a-a1fd-4f3c-81a0-f013753ad79f\tasks\a9744b08cf53e4f9c.output にJSONL形式で残っている可能性あり（超巨大ファイルの恐れ。読むなら tail などで末尾だけ）。**⚠このエージェントをSendMessageで再開するとセッションごと落ちる事象が2回連続で起きた。再開せず、新セッションで新規に（WebSearch中心・WebFetch少なめ・対象3件程度に絞って）やり直すこと**
4. Baymardの load more 記事本文の裏取り（できれば）
5. **最終レポート**: 実測した問題点 × 他サイト実例 × 指針原典 を突き合わせ、案A/B/Cを根拠付きで組み直してユーザーに提示。「他サイトの真似」ではなくTCGYMの前提との差分（6店舗比較・デッキ起点・レアリティ13種問題）を明示して採否を書く

## 6. クラッシュに関するメモ（新セッションへの注意）

- 症状: バックグラウンドエージェント（WebSearch/WebFetch多用の「国内TCG相場サイト洗い出し」）実行中にClaude Codeプロセスごと再起動が3回。相関であって因果は未確定
- 対策案: ①その種の調査はエージェントに投げず本体で小刻みにやる ②投げるなら対象を絞る（3サイト・WebFetch数回まで） ③ブラウザ調査（mcp__Claude_Browser）は本体でやる分には問題は起きていない
- 旧エージェントID（a678d9845b55cfc1b=指針調査・完了済み／a9744b08cf53e4f9c=国内調査・未完）は**新セッションから再開しないこと**

## 7. ユーザーとの合意事項（重要）

- 改善案は選択肢で提示し判断を仰ぐ（CLAUDE.mdルール）。実装はまだ何も着手していない
- 「同種サイトは判断材料であって正解ではない。縛られない」
- 報告は補助輪付き（平易な要約→技術詳細）・横文字に括弧補足

---

## 8. 完了: 価格.com 実地計測（2026-08-25・スマホ幅375px・私のブラウザで確認済み）

計測対象 = `s.kakaku.com`（スマホ専用サイト）／製品 = Apple AirPods Pro 3（**37ショップ掲載**の多店舗製品）
※ `kakaku.com` 直下はPC固定幅レイアウトが返るため計測に使えない。スマホ版は `s.` サブドメイン。

### 8-1. 商品ページの縦順（y座標・実測）

| y | 高さ | 内容 |
|---|---|---|
| 94 | 40 | 製品名 |
| **142** | 26 | **最安価格 ¥33,600 ＋「価格推移」リンク** |
| 214 | 227 | 商品画像パネル |
| **457** | 3,367 | **価格比較（店舗一覧）セクション開始** |
| 3,824 | 356 | よく比較される製品 |
| 4,179 | 856 | レビュー |
| 5,036 | 581 | クチコミ |
| **5,616** | 1,141 | **製品情報（説明文）** |
| 6,757 | 1,038 | スペック・仕様 |

- **最安値が y=142（1画面目の上部）**、**店舗一覧が y=457（1画面目の下端＝0.6画面）**
- **価格推移グラフはインライン展開せずモーダル**（`p-modalPricehistory`）。リンク1行だけ置く
- **製品説明文は y=5,616（約7画面下）**。TCGYMが1画面目に置いている「効果テキスト」に相当するものを、価格.comは価格情報より**後ろ**に置いている
- 全高 10,913px（TCGYM検索結果 34,215px の約1/3）

→ TCGYM実測（ヒーロー338px＋グラフ286px＋サマリー765px＋並べ替え94px＋レアリティ252px → 一覧先頭 y=2,248）と**同じ「価格を先に出す」問題への逆の解答**。

### 8-2. 店舗一覧の既定ソートと構造（TCGYMへの示唆が最も大きい部分）

- **並べ替えUIが存在しない**。順序は**常に価格昇順で固定**
- 代わりにあるのは「**表示価格の定義**」切替: `条件なし` / `送料込み（商品価格＋送料）` / `ポイント差し引き（商品価格＋送料−ポイント）`
  → ソート軸を選ばせるのではなく、**比較する金額の定義**を選ばせている
- 一覧は**目的別にグルーピング**して先頭に要約:
  1. 最安値ショップ（1枚）
  2. **クレジットカード最安**（1枚・軸違いの最安を別掲）
  3. ショッピングモール別価格（楽天 / Amazon）
  4. 量販店別価格（Joshin / エディオン / ヨドバシ …）
- 各行に**順位番号**（同価格は同順位。例: 31位が6店舗並ぶ）と**最安との差額**「¥42,800（**+9,200**）」を併記
- 絞り込みは横スクロールのタグ4個（`条件なし / クレカ対応 / 在庫あり / 延長保証`）＋詳細は「絞り込み」モーダル
- **37店舗中10店舗だけ表示 → 「他 27 店舗を見る」ボタン**（load more）
- **内部スクロールは無し**（`.p-shop` は overflow:visible / max-height:none）。ページスクロールと二重にしていない

### 8-3. 検索結果ページ（`search.kakaku.com`・スマホ幅）

- 1製品1行に「最安値 33,600円（**37店舗**）★4.36（68）」＋**上位4店舗を価格昇順でインライン表示**（モバイル一番33,600 / ハル・システム35,000 / 楽天市場36,800 / Amazon 36,800）
- つまり**検索結果の段階で既に価格順の店舗要約が見える**

### 8-4. TCGYMとの差分（採否判断用・「真似る」ではなく差を見る）

| 論点 | 価格.com | TCGYM現状 | 差の性質 |
|---|---|---|---|
| 店舗一覧の順序 | 価格昇順で固定・並べ替えUIなし | サーバー返却順（`by_shop`のObject.entries順） | **TCGYM側が明確に劣る。前提の違いではない** |
| 最安との差額表示 | 全行に「(+9,200)」 | なし | 同上。6店舗比較が売りなら効果は大きい |
| 一覧の件数 | 10件＋「他27店舗」 | 184行を平積み | 同上 |
| 内部スクロール | なし | PCで max-height:324px の二重スクロール | 同上 |
| 説明文の位置 | 価格の後ろ（y=5,616） | 価格の前（1画面目・338px） | 前提差あり: 遊戯王の効果テキストは「その1枚か」の同定に要る場合がある → **全出しではなく折りたたみ**が落とし所 |
| 価格推移グラフ | モーダル（1行のリンク） | インライン286px | 同上。折りたたみ or モーダル化の根拠になる |
| 絞り込み | タグ4個＋モーダル | レアリティ14ボタンを常時展開252px | **前提差あり**: TCGYMはレアリティが13種＋同一カードで価格が桁違い＝絞り込みが必須級。ただし「常時14ボタン展開」である必要はない |
| 軸違いの最安を別掲 | 「クレジットカード最安」を独立表示 | なし | 示唆: TCGYMなら「**レアリティ別の最安**」を同じ形で別掲できる（#rgrid昇格案A2と接続） |

**この節の結論**: 価格.comは「6店舗を横断比較する」という点でTCGYMと**前提がほぼ同じ**（むしろ店舗数は多い）。よって 8-2 の知見は前提差を理由に割り引く必要がなく、案A2（店舗サマリーを価格順）・A1（順序入替）・A3（既定20行＋もっと見る）の**直接的な裏付け**になる。

---

## 9. 完了: 国内TCG価格比較サイト 実地計測（2026-08-25・スマホ幅375px）

WebSearchで洗い出した国内の直接競合5件（ポケカジラ / トレカHACK / 遊戯王相場検索 / トレカプライス / トレカネット / eスターボックス）のうち、
TCGYMと構成が近い順に3件を選定 → うち2件を実測。

| サイト | 選定理由 | 結果 |
|---|---|---|
| eスターボックス `tcgkakaku.e-starbox.com` | 遊戯王で**販売＋買取の両方**を扱う＝TCGYMと最も近い | **実測済** |
| トレカプライス `tcgprice.jp` | 高騰/下落ランキングを持つ＝TCGYMの騰落タブと同じ | **実測済** |
| トレカネット `toreca.net` | デッキレシピ＋価格比較＝TCGYMのデッキ起点と同じ | **取得不可**（ブラウザのナビゲーションが拒否。要再挑戦 or 諦め） |

### 9-1. eスターボックス — カードページ（灰流うらら 20TH-JPC85 20thシークレット／全高 3,703px）

| y | 内容 |
|---|---|
| 0 | ヘッダー **64px**（ロゴ＋ハンバーガー「メニュー」のみ。**タブ列なし**） |
| 94–193 | カード名 / 型番＋レアリティ / 収録弾 |
| 233 | 見出し「カード概要」 |
| **287** | 概要カード303px = 画像＋**販売価格 ¥89,800 ＋ トリム平均**（外れ値を除いた平均） |
| 662 | 見出し「価格チャート」 |
| 716 | チャート canvas 300px（**インライン**） |
| **1,480** | **販売価格一覧**（テーブル＋テーブル内「検索:」フィルタ） |
| **1,721** | **買取価格一覧**（同一ページに縦並び） |
| **2,361** | **カード情報（効果テキスト・ステータス）** |
| 2,825 | 関連カード |

**重要な発見**
1. **URLが「カード番号＋レアリティ」単位**（`card?no=20TH/JPC8520SE`）。1ページ＝1カード×1レアリティ。TCGplayerと同じ方向で、**TCGYMの「1ページに13レアリティ436件を平積み」と対照的**
2. **販売と買取が同じページに縦並び**。TCGYMはこれを**タブで分離**している ← 案Bの「販売⇄買取をセグメント切替」より踏み込んだ選択肢（**同一ページ併置**）が実在する
3. **効果テキストは価格より後ろ**（y=2,361）。価格.comの「製品情報を後ろ」と一致（独立2例）
4. 価格チャートは**インラインだが概要の後ろ**（価格.comはモーダル）。＝グラフの扱いは2サイトで割れている
5. 生の最安値だけでなく**トリム平均**を併記（外れ値対策の統計量）
6. トップページが**充実**: 注目度／販売高騰／販売下落／買取高騰／買取下落の5ランキング＋新商品情報＋人気検索キーワード＋掲載ショップ一覧（**TCGYMのトップ「カード名を入力して検索」1行と対照的**）
   - ただし**当日の前日差は ¥0 や「‐」が並んでいた**。TCGYMの movers 空問題と同種の弱さは競合にもある＝「毎日必ず動きが出る」前提は置けない

**TCGYMが勝っている点（公平のため）**: eスターボックスはこのカードで**掲載が1ショップ（トレコロ）のみ**。TCGYMの6店舗横断の方がカバレッジは明確に厚い。**構成は学べるがデータ量では負けていない**。

### 9-2. トレカプライス — カードページ（全高 9,170px）

| y | 内容 |
|---|---|
| 0 | ヘッダー32px＋ナビ80px（**HOME / サイトの使い方 / お気に入りカード の3個のみ**） |
| 386 | カード名 H1（ヘッダー〜H1の間に386px＝広告等） |
| 2,136 | 見出し「概要」 |
| **2,807** | **収録弾（H2）** |
| **2,910** | **レアリティ（H3）** |
| **3,175** | **価格統計**（最安値／中央値／平均値／最高値）＋その下に型番×価格×ショップの表 |
| 7,166 | 関連記事 |

**重要な発見**
1. **収録弾（H2）→レアリティ（H3）→統計→店舗表 の入れ子**。1ページ内で扱うが**レアリティごとに区切って**いる。TCGYMの平積みへの、国内における別解
2. 各グループ先頭に**4指標の要約**（最安・中央・平均・最高）。**中央値**を出すのは同一カードで価格が散らばるTCG特有の事情への対処
3. **価格表が y=3,175（約4画面下）** — **TCGYMの y=2,248 より悪い**。トップページも全高13,449px、検索窓が y=864（TCGYMは y=293）
   → **反面事例**。「国内競合がやっているから正しい」わけではないことの実例として使う

### 9-3. 国内3件から得た採否材料（まとめ）

| 論点 | 国内の実例 | TCGYMへの示唆 |
|---|---|---|
| レアリティの扱い | e-star=URL分割 / トレカプライス=ページ内で見出し区切り | **平積み以外の解が2通り実在**。案A3（既定=選択レアリティ）より踏み込んだ「レアリティ別に区切る」も選択肢 |
| 効果テキストの位置 | e-star=価格の後ろ（価格.comも同じ） | **独立3例が「説明文は価格の後」**。案A1の折りたたみ案を後押し |
| 販売と買取 | e-star=同一ページに縦並び | 案Bの「セグメント切替」に加え「**同一ページ併置**」という第3案 |
| 価格の要約統計 | e-star=トリム平均 / トレカプライス=最安・中央・平均・最高 | TCGYMは最安のみ。**中央値/トリム平均は「外れ値の高額出品に引っ張られない相場」**を出せる（レアリティ混在問題の緩和にもなる） |
| グラフの位置 | e-star=インライン(2画面目) / 価格.com=モーダル | **割れている**。＝「必ず折りたたむべき」とまでは言えない。ただし**1画面目に置く例は無し** |
| トップの空状態 | e-star=5ランキング＋新商品＋人気キーワードで充実 | 案A6を後押し。**ただし競合も当日差分は¥0だらけ** → 「騰落」以外の常に埋まる素材（人気検索キーワード・掲載ショップ一覧・発売スケジュール）を混ぜるのが現実的 |
| ナビゲーション | e-star=ハンバーガーのみ（タブ列なし・ヘッダー64px） / トレカプライス=3個 | **国内競合はどこも横タブ7個をやっていない**。TCGYMヘッダー135px＋タブ3段144pxは国内でも突出して重い |

---

## 10. 完了: Baymard「load more vs pagination vs 無限スクロール」の裏取り（2026-08-25）

- 旧セッションで本文取得に失敗していた件。**baymard.com ドメイン内の検索で該当記事を特定**:
  `baymard.com/blog/external-load-more-vs-pagination-vs-infinite-scrolling`（"Testing Pagination Against Infinite Scrolling and 'Load More' Buttons"）
- 出典元 = Baymard の "Product Lists & Filtering" ユーザビリティ調査（PC・スマホ両方、カテゴリ一覧と検索結果の両方）
- 要旨:
  - **「もっと見る」ボタンが3方式の中で最も成績が良かった**（PC・スマホ共通）。ただし実装を誤ると利得が相殺される落とし穴あり
  - **ページ送り**: 遅いと感じられ、リンクが多数並ぶと一覧を最後まで見る意欲を削ぐ
  - **無限スクロール**: 走査量は増えるが1件あたりの注視が減る／フッターに到達できない／追加され続けて圧倒される／**戻ったとき元の位置に戻れない問題が深刻**
- **確度**: 記事タイトル・出典調査名・要旨まで一次ドメインで確認。ただし**本文の全文取得はしていない**（検索結果スニペット経由）。数値（%やテスト対象サイト数）は未確認
- → 案A3「既定20行＋もっと見る」は**方式として支持される**。無限スクロール化は選ぶべきでない

---

## 11. 残タスクの最終状態（2026-08-25 時点）

| # | 項目 | 状態 |
|---|---|---|
| 1 | Cardmarket | **見送り**（Cloudflare bot検証。国内2件＋価格.com＋TCGplayerで十分な材料が揃ったため費用対効果で打ち切り） |
| 2 | 価格.com | **完了** → §8 |
| 3 | 国内TCG相場サイト | **完了（2/3）** → §9。トレカネット(toreca.net)のみ取得不可 |
| 4 | Baymard load more | **完了** → §10 |
| 5 | 最終レポート | **完了** → `docs/ux-survey-2026-08-25-report.md` |

※ 旧セッションのクラッシュ事象は再現せず。ブラウザ調査（mcp__Claude_Browser）＋本体WebSearchの組み合わせでは一度も落ちなかった。
